baseURL = 'https://tangkk.github.io/lobster-heartvoices/'
languageCode = 'zh-cn'
title = '龙虾心声文字版'
defaultContentLanguage = 'zh'

[params]
  description = '把龙虾心声播客内容整理成可阅读、可订阅的文字文章。'

[outputs]
  home = ['HTML', 'RSS']
  section = ['HTML', 'RSS']

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
